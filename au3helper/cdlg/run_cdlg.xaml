<ContentDialog
    x:Class="au3helper.run_cdlg"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:au3helper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="运行程序"
    PrimaryButtonText="插入"
    SecondaryButtonText="关闭"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid>
            <StackPanel>
                <TextBlock>运行程序</TextBlock>
                <TextBlock TextWrapping="Wrap" Foreground="{ThemeResource HyperlinkForegroundThemeBrush}">这里为脚本运行的程序，并非在此应用进行调用。</TextBlock>
                <TextBox x:Name="run_prog" Margin="0,4"/>
            </StackPanel>
        </Grid>
    </Grid>
</ContentDialog>
